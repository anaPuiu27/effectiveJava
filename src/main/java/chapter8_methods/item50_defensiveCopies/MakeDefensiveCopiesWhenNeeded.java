package chapter8_methods.item50_defensiveCopies;

public class MakeDefensiveCopiesWhenNeeded {

    //You must program defensively, with the assumption that clients of your class will do their best to destroy its invariants
    //Date is obsolete and should no longer be used in new code.
    //make a defensive copy of each mutable parameter to the constructor and validate the copies
    //accessors return defensive copies of mutable internal fields
    //try use immutable objects as components of your objects so that you that don’t have to worry about defensive copying
}
